services:
  - type: web
    name: floatchat-frontend
    env: static
    buildCommand: yarn install && yarn build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        sync: false
    autoDeploy: true
    buildFilter:
      paths:
        - frontend/**
    rootDir: frontend
